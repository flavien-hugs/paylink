{
  "name": "paylink-admin-web",
  "version": "1.0.0",
  "private": true,
  "description": "PayLink — back-office for payments, entities, users and reporting.",
  "type": "module",
  "scripts": {
    "dev": "vite dev --host --port 4173",
    "build": "vite build",
    "preview": "vite preview --host --port 4173",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/adapter-node": "^5.2.10",
    "@sveltejs/kit": "^2.8.0",
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@tailwindcss/vite": "^4.3.0",
    "@types/node": "^20.19.42",
    "svelte": "^5.1.0",
    "svelte-check": "^4.0.0",
    "tailwindcss": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@lucide/svelte": "^1.17.0"
  }
}
